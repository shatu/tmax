# Remaining release work

This branch is **not** the complete `tblite-clean` dataset release and its
oracle checks are **not** the final checkpoint scores.

1. Rulin has sealed the full task/bootstrap bundle. Encrypted transfer through
   the existing private draft release is requested (issue #1 comment 5624022203).
   Verify archive and internal checksums before assembly. Preserve accepted
   DB/curl and bloom/redis repairs; do not import deferred broad guards. We do
   not yet have the decrypted bundle.
2. ETL synchronous startup passed Oscar's real-agent smoke 5/5. The full series
   completed with 116/155 scored and 39 unresolved; diagnose original verifier
   timeouts separately from retry upload errors. Hyak's user-mapping limitation
   remains a site limitation, not a model zero. Keep ACL excluded, not ETL.
3. Complete lower-concurrency backfills on reviewed fresh-worker pins. Hydra's
   old-backend series had 70/155 scored, MLflow 21/155; they are not complete
   measurements. Maven/OkHttp build handoff is in BUILD_HANDOFF.md. Retain exact
   React agent manifests before verification. The expanded cache passed Hyak's
   full Harbor oracle (17/17 assertions, reward 1); destination model reruns
   remain. Do not turn missing/infra outcomes into model zeros.
4. Reconcile the latest canonical attempt table with immutable fills before
   publishing scores. The old table's missing/unscored rows are not a new rerun
   list. Preserve fractional rewards, attempt identities and image/task variants;
   never impute missing trials as zero or mix oracle diagnostics into scores.
5. Assemble all 99 included full task directories and an explicit ACL exclusion
   using the pinned upstream and verified repairs/assets; verify the resulting
   package and publish consistent-coverage checkpoint scores/error bars.

Local independent work completed: self-contained Maven, OkHttp, MLflow, React,
Hydra and ETL candidate contexts; offline oracle checks for all six; full Hyak
acceptance for React/Hydra, and earlier acceptance of the original three
dependency-repair SIFs. See README and raw evidence for scope and limitations.
